const Title = ({title}) => {
    return (  
        <div className="Title">
            <div className="titleText">
                <h1>{title}</h1>
            </div>
        </div>
    );
}
 
export default Title;